[project]
name = "twiga-warehouse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pgvector>=0.3.6",
    "python-dotenv>=1.0.1",
    "pyyaml>=6.0.2",
    "sqlmodel>=0.0.22",
]
